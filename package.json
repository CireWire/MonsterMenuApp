{
  "name": "monster-menu-app",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "cd monster-menu && npm install && npm run build",
    "start": "cd monster-menu && npm run start"
  }
}
